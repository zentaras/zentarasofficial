generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model User {
  id        String   @id @default(cuid())
  clerkId   String   @unique @map("clerk_id")
  email     String   @unique
  firstName String?  @map("first_name")
  lastName  String?  @map("last_name")
  imageUrl  String?  @map("image_url")
  username  String?  @unique
  createdAt DateTime @default(now()) @map("created_at")
  updatedAt DateTime @updatedAt @map("updated_at")

  dataAnalystApps  DataAnalystApplicant[]
  webDevApps       WebDevApplicant[]
  internshipTracks InternshipTrack[]

  @@map("users")
}

model DataAnalystApplicant {
  id             String    @id @default(cuid())
  clerkUserId    String?   @map("clerk_user_id")
  user           User?     @relation(fields: [clerkUserId], references: [clerkId])
  fullName       String    @map("full_name")
  email          String
  college        String
  branch         String
  graduationYear String    @map("graduation_year")
  cgpa           String?
  githubUrl      String    @map("github_url")
  resumeLink     String    @map("resume_link")
  status         String    @default("Pending")
  adminNote      String?   @map("admin_note") @db.Text
  reviewedAt     DateTime? @map("reviewed_at")
  reviewedBy     String?   @map("reviewed_by")
  createdAt      DateTime  @default(now()) @map("created_at")

  @@index([email])
  @@index([status])
  @@index([clerkUserId])
  @@map("data_analyst_applicants")
}

model WebDevApplicant {
  id             String    @id @default(cuid())
  clerkUserId    String?   @map("clerk_user_id")
  user           User?     @relation(fields: [clerkUserId], references: [clerkId])
  fullName       String    @map("full_name")
  email          String
  college        String
  branch         String
  graduationYear String    @map("graduation_year")
  cgpa           String?
  githubUrl      String    @map("github_url")
  resumeLink     String    @map("resume_link")
  status         String    @default("Pending")
  adminNote      String?   @map("admin_note") @db.Text
  reviewedAt     DateTime? @map("reviewed_at")
  reviewedBy     String?   @map("reviewed_by")
  createdAt      DateTime  @default(now()) @map("created_at")

  @@index([email])
  @@index([status])
  @@index([clerkUserId])
  @@map("web_dev_applicants")
}

model InternshipTrack {
  id                String    @id @default(cuid())
  clerkUserId       String    @map("clerk_user_id")
  user              User      @relation(fields: [clerkUserId], references: [clerkId])
  projectKey        String    @map("project_key")
  projectName       String    @map("project_name")
  applicantId       String    @map("applicant_id")
  currentStep       Int       @default(1) @map("current_step")
  isCompleted       Boolean   @default(false) @map("is_completed")
  completedAt       DateTime? @map("completed_at")
  performanceRating String?   @map("performance_rating")
  overallFeedback   String?   @map("overall_feedback") @db.Text
  certificateLink   String?   @map("certificate_link")
  lorLink           String?   @map("lor_link")
  projectRepoLink   String?   @map("project_repo_link")
  evaluationNotes   String?   @map("evaluation_notes") @db.Text
  createdAt         DateTime  @default(now()) @map("created_at")
  updatedAt         DateTime  @updatedAt @map("updated_at")
  steps             InternshipStep[]

  @@unique([clerkUserId, applicantId])
  @@index([clerkUserId])
  @@index([projectKey])
  @@map("internship_tracks")
}

model InternshipStep {
  id          String          @id @default(cuid())
  trackId     String          @map("track_id")
  track       InternshipTrack @relation(fields: [trackId], references: [id], onDelete: Cascade)
  stepNumber  Int             @map("step_number")
  stepTitle   String          @map("step_title")
  status      String          @default("pending")
  data        Json?
  adminNote   String?   @map("admin_note") @db.Text
  reviewedAt  DateTime? @map("reviewed_at")
  reviewedBy  String?   @map("reviewed_by")
  deadline    DateTime? @map("deadline")
  submittedAt DateTime? @map("submitted_at")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")

  @@unique([trackId, stepNumber])
  @@index([trackId])
  @@index([status])
  @@map("internship_steps")
}





Project Assignment: 
Build a dynamic candidate profile dashboard page. 
The page should display all user-specific information including profile details, 
internship status (ongoing/completed), skills, uploaded resume, and a week-wise activity 
timeline showing user progress.

The dashboard should support dynamic data rendering from APIs/database and be fully responsive 
across desktop, tablet, and mobile devices. Focus on clean UI/UX, reusable components, proper 
state management, loading/error states, and optimized frontend performance.



Project Requirements
-> Create a responsive user profile dashboard UI
-> Fetch and display dynamic user data from backend/API
Show:
 1. Profile image/avatar
 2. Full name
 3. Username
 4. Email
 5. Internship role/status
 6. Skills/tags
 7. Activity timeline
 8. Assigned tasks/projects
 9. Certificate link
-> Add loading states and error handling
-> Add hover effects, transitions, and clean UI interactions (not too heavy)
-> Maintain consistent dark theme styling
-> Ensure mobile responsiveness
-> Use reusable components and clean folder structure
-> Optimize performance and avoid unnecessary re-renders


Tech Stack & Tools 
React.js / Next.js
Tailwind CSS
Node.js + Express 
MongoDB / PostgreSQL
REST API integration
Git & GitHub
Axios / Fetch API
Vercel / Netlify deployment


Suggested Approach
-> Analyze existing dashboard structure and UI consistency
-> Create wireframe/layout for profile dashboard
-> Build reusable components:
 1. Profile Card
 2. Status Card
 4. Skills Section
 5. Timeline Section
 6. Activity Feed
-> Integrate backend APIs for dynamic data
-> Implement responsive design for all screen sizes
-> Add loading skeletons and error handling
-> Improve UI polish with animations and hover states
-> Test responsiveness, accessibility, and performance
-> Deploy final implementation and submit GitHub repository + live demo link